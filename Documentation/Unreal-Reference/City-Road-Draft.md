# Terrain-surface city road draft

Tested Unreal 5.8.2, 13 September 2026, 14:54. John requested roads only from CITY-PLAN.png.

Current saved map: `/Game/Conservatory/Maps/L_Exterior_RobotVR`. Unreal was already open, with no dirty map or content packages. `CityRoadDraftBaseline.json` records the actual live state before changes. The saved map timestamp matched the preceding tree integration. No map reload or generator was used over the live editor.

Created 38 road paths with a civic circle, central grid, west/east/south crescents, northern winding loop and four outer approaches. Eight roundabout islands complete the connections. Layout is centred at (285, -40) metres and fitted to the existing 1260 m terrain; the reference's illustrated kilometre scale and place names were not adopted. Existing town buildings and the combined old infrastructure mesh remain intact, including any conflicting old approaches. No river, buildings, landscape grading or traffic systems were added.

The roads are a reversible world-XY texture overlay in a duplicate of M_Ground. Original material remains `/Game/Conservatory/Environment/Materials/M_Ground`. Applied material: `/Game/Conservatory/Environment/CityRoadDraft/M_CityRoadDraft_v1`. The original graph is retained, with only Base Color blended to road and shoulder colours. Outside the mask it uses the original output. This is terrain-integrated surface appearance, not floating geometry, native editable Landscape weight layers, or engineered road grading. Terrain elevation and all 2186 actor transforms are unchanged.

Source: `SourceAssets/CityRoadDraft/RoadLayout.json`, `CityRoadMask.png`, `RoadDraft-Plan.png`; generator: `Scripts/make_city_road_draft.py`. The JSON records widths and curve control points. The current generator defines the layout in source and writes the JSON; edit the generator or adapt it to read revised JSON for future changes, then reimport T_CityRoadMask. Do not rerun the initial application script blindly over newer edits.

Mask is 4096 square over world (-630,-630) to (630,630) metres. R is pavement and G is pavement plus shoulder. Unreal uses `(AbsoluteWorldPosition.xy + 63000) / 126000` in centimetres; texture sRGB off, Masks compression, clamp addressing. The ComponentMask expression's input connection uses the empty pin name in this engine; `Input` failed before the map was modified. A saved unused M_CityRoadDraft experiment remains in the same new asset folder and is not assigned to the Landscape.

Backup: `Saved/Backups/CityRoadDraft/20260913-145104/L_Exterior_RobotVR.umap`. To remove the draft while retaining later actor work, restore the Landscape's original M_Ground assignment, rather than restoring the whole old map.

Verification: CityRoadDraftResult.json records exact actor-transform equality, material assignment and checkpoint. The saved map was reopened in a bounded windowed game, current material confirmed, and CityRoadDraftUnreal.png visually inspected. Existing actors are visible alongside the road pattern. Review exited normally. No traversal or nav rebuild was required for this colour-only draft. Roads may cross existing buildings/old infrastructure; these placements are intentionally deferred for John's later town work. The first camera capture faced sky because positional Rotator arguments were misordered; the final capture uses named pitch/yaw/roll values and shows the actual ground layout.

## Town hall positional — tested UE 5.8.2, 2026-09-13
Scripts/place_town_hall_placeholder.py placed 19 attached basic-shape parts at (31250, 5300, 896.406) cm after sampling the current landscape at nine footprint points. Footprint 38 x 30 m fits the block north of the civic circle, east of the north avenue. Current user-flattened terrain preserved; 2140 prior actor transforms verified unchanged. Map saved, editor visual check passed with road clearance. Evidence: Documentation/TownHallPositional.json. Initial-only script intentionally refuses a duplicate; do not rerun to update existing parts. This is a scale placeholder without finished facade or interior.


## Removal — tested UE 5.8.2, 14 September 2026
John superseded this city draft with a rural direction. Scripts/remove_city_for_rural.py restored M_Ground and removed remaining actors in the legacy 03 Village folders and the tagged town-hall folder. Current buildings/infrastructure meshes were already absent following John's edits; remaining props, walls, fill lights and unused clue anchors were cleared. Source assets remain available. Saved/reopened map retains all 2,023 other actor transforms. Evidence: ../RuralRemovalResult.json; full pre-edit map checkpoint in Saved/Backups/RuralRemoval/20260914-110438. No terrain height editing or old-map restoration was performed. Commandlet Python script paths must use forward slashes: backslash followed by 1 was interpreted as an escape in this session. Runtime visual review uses Scripts/review_rural_removal.py. This does not establish a new traversal test or finished rural art.
`nRuntime overview Documentation/RuralClearedOverview.png visually inspected: open terrain with no city massing or road overlay in the former city area. Review exited normally at 11:05:27 local. No editor remains open or unsaved.
